<% if user_signed_in? %>
Logged in as <strong><%= current_user.email %></strong>.
<%= render 'leavedates/leavedate_form' %>
<% else %>
<div class="center hero-unit">
  <h1>Welcome to the Leave Tracker</h1>

  <h2>
    This is the home page for the Leave Tracker.
  </h2>
</div>
<% end %>