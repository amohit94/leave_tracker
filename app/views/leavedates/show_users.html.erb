<% provide(:title, 'All users') %>
<h1>All users</h1>
<% @users = User.all %>
<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= link_to user.name.to_s+" <"+user.email.to_s+">", "users/"+user.id.to_s %>
    </li>
  <% end %>
</ul>