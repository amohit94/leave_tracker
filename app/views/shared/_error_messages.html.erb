<% if @leavedate and @leavedate.errors and @leavedate.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-error">
      The form contains <%= pluralize(@leavedate.errors.count, "error") %>.
    </div>
    <ul>
    <% @leavedate.errors.full_messages.each do |msg| %>
      <li>* <%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>